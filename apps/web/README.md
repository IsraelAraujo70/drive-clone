# Web App

TypeScript frontend for the Google Drive clone.

This directory will contain the browser application for authentication, file browsing, uploads, downloads, sharing, search, and storage usage.
