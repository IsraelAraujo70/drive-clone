meta {
  name: Root
  type: http
  seq: 1
}
get {
  url: {{baseUrl}}/
  body: none
  auth: none
}
headers {
  Accept: application/json
}

tests {
  test("response status is documented", function() {
    expect(res.status).to.equal(200);
  });
}

docs {
  Returns the API service identity and product message.
}
